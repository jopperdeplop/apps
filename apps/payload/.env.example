# Database Connection
# Replace with your Neon connection string for 'payload_cms'
DATABASE_URL=postgres://user:password@ep-cool-project-123456.us-east-1.aws.neon.tech/payload_cms?sslmode=require

# Payload Secret
# Used to encrypt sessions and tokens. Generate a random string (e.g. `openssl rand -hex 32`)
PAYLOAD_SECRET=your-secret-key-here

# Public Server URL
# Used for generating absolute links. In dev it is localhost.
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Vercel Blob (Optional - for media storage)
# BLOB_READ_WRITE_TOKEN=
